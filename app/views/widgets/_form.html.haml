= form_for @widget || Widget.new, validate: true do |f|
  = f.label :name
  = f.text_field :name    
  = f.label :body
  = f.text_area :body

  -#%fieldset{'data-recieves-errors': 'expires_on'}
  -#= f.select :expires_on_year
  -#= f.select :expires_on_month
  -#= f.select :expires_on_date
  -#= f.hidden_field :expires_on

  = f.submit
